import React from 'react';
import { createRoot } from 'react-dom/client';
import ProspectTracker from './ProspectTracker.jsx';

createRoot(document.getElementById('root')).render(<ProspectTracker />);
